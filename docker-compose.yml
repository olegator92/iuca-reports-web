version: "3.9"

services:
  web:
    image: ghcr.io/olegator92/iuca-reports-web/web:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    environment:
      - NODE_ENV=production
    restart: unless-stopped
