version: "3.9"

services:
  web:
    image: ghcr.io/olegator92/iuca-reports-web/web:dev
    build:
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
